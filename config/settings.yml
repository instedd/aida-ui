version: 0.4.0
backend:
  url:
  public_socket_url:

mailer:
  from: 'aida@instedd.org'
  delivery_method: smtp

smtp_settings:
  # user_name:
  # password:
  # address:
  # domain:
  # port:
  # authentication:

base_url:
